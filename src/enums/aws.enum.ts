export enum AWS_PLATFORM {
  LINUX_UNIX = 'LINUX_UNIX',
  WINDOWS = 'WINDOWS'
}

export enum AWS_PLATFORM_TYPE {
  OS = 'os',
  APP = 'app'
}

export enum AWS_INSTANCE_STATUS {
  RUNNING = 'running',
  STOPPED = 'stopped',
  STOPPING = 'stopping',
  PENDING = 'pending'
}
